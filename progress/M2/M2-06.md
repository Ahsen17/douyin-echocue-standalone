# M2-06 安全与路由基准测试（开发部分）

> **真实情况标注**：本任务完整完成需要甲方真实/脱敏样本（版本化数据集、漏放/误杀真实数字、失败 case 分析与参数微调、甲方签核）。**截至本批次仅完成「开发部分」**（评测工具），数据验证/参数微调/甲方签核待甲方样本到位后收尾。示例数据仅用于验证工具可重复执行，**不是 POC 通过证据**。

## 任务信息

| 字段 | 内容 |
|------|------|
| 任务 ID | M2-06 |
| 任务名称 | 执行安全与路由基准测试 |
| 状态 | ⏳ 进行中（开发部分已完成；数据验证/微调/签核待甲方样本） |
| 完成时间 | - |
| 分支 | feat/M2-06 |

## 产出文件（开发部分）

- `src/main/benchmark/types.ts` — 版本化样本数据集类型（对齐 POC 模板 §2 字段）
- `src/main/benchmark/samples.ts` — `validateBenchmarkSamples`（含跨字段一致性校验）+ `hashDataset`（键序无关 SHA-256）
- `src/main/benchmark/safety-benchmark.ts` — `runSafetyBenchmark` + `runFailClosedCheck`
- `src/main/benchmark/routing-benchmark.ts` — `runRoutingBenchmark`
- `src/main/benchmark/report.ts` — POC §3/§4 markdown 报告生成器
- `src/main/benchmark/index.ts` — 模块导出
- `scripts/run-safety-routing-benchmark.ts` — CLI（tsx，--samples/--policy/--personas）
- `docs/06-data-interface/fixtures/safety-routing-benchmark-{samples,policy,personas}-v1.json` — 示例数据集/策略/词表
- `tests/unit/benchmark/{samples,safety-benchmark,routing-benchmark,report}.test.ts` — 33 项单元测试
- `package.json` — `benchmark:safety-routing` 脚本；`tsconfig.node.json` — 纳入 `scripts` 类型检查

## 实现说明

### 运行方法（示例数据）
```bash
npm run benchmark:safety-routing
# 或指定真实样本/策略/词表
npm run benchmark:safety-routing -- --samples <真实样本.json> --policy <编译规则.json> --personas <词表.json>
```

### 测试命令
```bash
npm run typecheck
npm run test:contracts
npm test -- tests/unit/benchmark
```

## 完成标准验收（开发部分）

- [x] 版本化样本数据集校验：schemaVersion/datasetId/caseId 唯一/枚举合法/跨字段一致性（filter 必须带 reason、allow 必须不带）
- [x] `hashDataset`：键序无关 SHA-256（数据集版本化/报告引用）
- [x] 安全基准：漏放(miss)/误杀(false_positive)/错误reason(wrong_reason) 分类；按类别聚合应拦截/实际拦截/漏放/误杀（POC §3 表）
- [x] fail-closed 专项：compiledRules=null 时每条必须 `SAFETY_ENGINE_ERROR`
- [x] 路由基准：对比 expectedPersonaId + 可选 expectedRouteDecision；按 scenario 聚合，保守处理（principal_fallback）计数（POC §4 表）；仅含 expectedPersonaId!=null 的样本
- [x] 报告：§3/§4 表 + fail-closed + 失败 case 清单（仅 caseId + 期望/实际，**不含样本原文**）
- [x] CLI：参数守卫 + 用法提示；示例数据全绿（安全 11/11、fail-closed 11/11、路由 6/6）
- [x] 示例 fixture 明确标注"示例/演示、非真实 POC 证据"；补齐 POC §2 `expected_semantic_type` 字段
- [x] 隐私：报告/错误消息不含弹幕原文、人设、昵称

## 未完成（待甲方数据）

- [ ] 甲方真实/脱敏样本入库并跑出真实漏放/误杀/路由准确率数字
- [ ] 失败 case 逐条分析 → 微调 M2-04 内置词表 / M2-05 `FUZZY_MATCH_THRESHOLD`
- [ ] 生成正式 POC 报告 + 甲方签核 + 技术复核（示例数据不能作为通过证据）
- [ ] 更新本节状态为完成并补录真实 artifact 引用

## 关键不变量

- 示例/mock/fixture 不是实际 POC 证据（路图 §1.6）；真实准确率报告须在甲方样本上补录
- 评测工具为纯函数、可重复执行；报告仅含类别/计数/失败 caseId，不含样本原文
- 输入安全过滤的 P0 目标：测试集内漏放为 0；引擎异常必须 fail closed 记录 `SAFETY_ENGINE_ERROR`
- 低置信/歧义路由进入保守路径（principal_fallback），不得让 LLM 决定身份

## 自检记录

SubAgent 严格自检（§3.2a）通过（PASS）。按报告修复 1 中 + 5 低 + 2 可选：跨字段一致性校验（filter↔reason）、报告标签"样本内容 SHA-256" + 合计行累加、边界测试（空样本/空 personas/全 safety）、ROUTE_DECISIONS readonly、示例数据补齐 expected_semantic_type、CLI 参数守卫 + 用法提示、routing conservative 改按 route.decision 统计、补路由失败清单渲染与键序无关哈希测试。

## 追溯

- Testcase: T-SAFE-001、T-PER-001
- Acceptance: A-03、A-04（**未完成**——需甲方样本真实证据 + 签核）
- 最小阅读包: 安全与路由 POC 模板（docs/03-research/Echocue-安全规则与路由POC记录模板-v0.1.md）
