# M0-04 进度总结

## 任务
锁定依赖并建立许可证/SBOM 入口

## 完成时间
2026-08-21

## 做了什么
- 将 package.json 所有 `^` 版本前缀改为精确版本，确保 `npm ci` 严格复现构建
- 添加 `license-checker@25.0.1` 和 `@cyclonedx/cyclonedx-npm@6.0.1` devDependencies
- 新增 npm scripts: `license:scan`、`license:check`、`sbom`、`compliance`
- 创建 `scripts/license-check.js`：扫描 582 个包，检测禁用许可证（GPL/AGPL/LGPL），违规时非零退出
- 新增 `tests/unit/pkg-compliance.test.ts`（T-PKG-001）：4 项测试全部通过
- 更新 `.github/workflows/test-windows.yml`：许可证检查 + SBOM 生成 + artifact 上传

## 产出文件
- `package.json`（精确版本 + 新脚本 + 新 devDeps）
- `package-lock.json`（更新）
- `scripts/license-check.js`
- `tests/unit/pkg-compliance.test.ts`
- `.github/workflows/test-windows.yml`（更新）

## 验证结果
- `npm run compliance`：582 包扫描，无违规许可证，sbom.cdx.json 生成成功
- `npm run test:unit`：4/4 通过
- `npm run typecheck`：通过，无错误

## 追溯
- W1、T-PKG-001、A-09
