# M0-03 任务完成报告

## 任务信息

- **任务 ID**: M0-03
- **任务名称**: 建立 Unit/Contract/Integration/E2E 测试骨架
- **完成日期**: 2026-08-21
- **前置任务**: M0-01, M0-02

## Summary

完成四层自动化测试框架搭建，为后续所有开发任务建立可验证的测试入口。核心产出包括：

1. **测试框架**: 引入 Vitest 2.1.9 + coverage-v8，配置全局环境和 alias
2. **目录结构**: 建立 tests/{unit,contract,integration,e2e,fixtures,setup}/ 分层目录
3. **Fixture Loader**: 实现统一的 JSON/JSONL fixture 加载器，支持类型安全
4. **Testcase 占位**: 创建 10 个 T-* testcase 占位文件（4 contract + 3 integration + 2 e2e + 1 unit）
5. **测试脚本**: 新增 8 个 npm scripts（test/test:unit/contract/integration/e2e/watch/coverage/all）
6. **Windows CI**: 创建 GitHub Actions workflow，确保 Integration/E2E 在 Windows x64 环境运行
7. **兼容性**: 保留现有 `test:contracts` 和 schemas.test.ts（40 个测试持续通过）

## 产出文件列表

### 配置文件
- `vitest.config.ts` — Vitest 主配置，设置 alias、coverage、环境
- `.github/workflows/test-windows.yml` — Windows CI workflow

### 测试基础设施
- `tests/setup/vitest.setup.ts` — 全局测试生命周期 hooks
- `tests/setup/helpers.ts` — 通用断言函数（expectValid/expectInvalid）
- `tests/fixtures/loader.ts` — Fixture 加载器（loadJsonFixture/loadJsonlFixture）
- `tests/fixtures/index.ts` — Fixture 导出入口

### Testcase 占位文件

**Contract 层（4 个）**:
- `tests/contract/T-CON-001-ws-event-fixtures.test.ts` — WebSocket 事件 fixtures（5 todo）
- `tests/contract/T-PROV-001-provider-contract.test.ts` — Provider 契约（1 实际 + 5 todo）
- `tests/contract/T-SAFE-001-safety-fixtures.test.ts` — 安全策略 fixtures（1 实际 + 5 todo）
- `tests/contract/T-DIAG-001-diagnostic-privacy.test.ts` — 诊断数据隐私（5 todo）

**Integration 层（3 个）**:
- `tests/integration/T-AUD-001-audit-storage.test.ts` — 审计存储（5 todo）
- `tests/integration/T-STO-001-sqlite-wal.test.ts` — SQLite WAL 与恢复（5 todo）
- `tests/integration/T-CON-002-ws-lifecycle.test.ts` — WebSocket 生命周期（5 todo）

**E2E 层（2 个）**:
- `tests/e2e/T-OVR-001-overlay-window.test.ts` — 浮窗行为（5 todo）
- `tests/e2e/T-PKG-001-windows-install.test.ts` — Windows 安装包（5 todo）

**Unit 层**:
- `tests/unit/.gitkeep` — 占位目录（后续任务填充）

### Package.json 脚本更新

新增脚本：
```json
"test": "vitest run",
"test:unit": "vitest run tests/unit",
"test:contract": "vitest run tests/contract",
"test:integration": "vitest run tests/integration",
"test:e2e": "vitest run tests/e2e",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage",
"test:all": "npm run test:contracts && npm run test"
```

## 测试命令

### 运行测试
```bash
# 运行所有 Vitest 测试
npm run test

# 分层运行
npm run test:unit          # Unit 测试（当前为空）
npm run test:contract      # Contract 测试（2 passed, 20 todo）
npm run test:integration   # Integration 测试（15 todo）
npm run test:e2e           # E2E 测试（10 todo）

# 运行现有 contracts 测试（独立）
npm run test:contracts     # 40 passed

# 运行全部测试（contracts + vitest）
npm run test:all

# Watch 模式
npm run test:watch

# 生成覆盖率报告
npm run test:coverage
```

### 验证通过情况

✅ **Contract 层**: 2 个测试通过（fixture loader 验证），20 个 todo 标记
```
Test Files  2 passed | 2 skipped (4)
Tests       2 passed | 20 todo (22)
Duration    380ms
```

✅ **现有 contracts 测试**: 40 个测试全部通过
```
40 tests: 40 passed, 0 failed
```

✅ **类型检查**: 无错误
```
tsc --noEmit && tsc --noEmit -p tsconfig.node.json
```

## 完成标准对照

| 标准 | 状态 |
|---|---|
| Vitest 配置文件存在且有效 | ✅ vitest.config.ts |
| 四层测试目录存在 | ✅ unit/contract/integration/e2e |
| Fixture loader 实现 | ✅ 支持 JSON/JSONL，能加载 3 个现有 fixture |
| 所有 T-* testcase 有占位文件 | ✅ 10 个文件（contract 4 + integration 3 + e2e 2 + unit 占位） |
| package.json 包含测试脚本 | ✅ 8 个脚本（test/unit/contract/integration/e2e/watch/coverage/all） |
| test:contracts 保持可用 | ✅ 40 个测试通过 |
| Windows CI workflow 存在 | ✅ .github/workflows/test-windows.yml |
| npm run test:all 无编译错误 | ✅ 通过（TODO 测试标记为 pending） |
| 测试失败返回非零退出码 | ✅ Vitest 默认行为验证 |
| npm run typecheck 通过 | ✅ 无错误 |

## 已知限制

1. **Testcase 实现留空**: 所有 T-* 测试文件仅占位（45 个 todo），实际测试逻辑由 M1-M7 任务实现
2. **Unit 层空目录**: 仅 .gitkeep 占位，待业务逻辑模块完成后填充
3. **Electron E2E 工具未引入**: M6 任务引入 Playwright/Spectron 时需扩展配置
4. **SQLite/Qdrant 未启动**: Integration 测试在 M1/M3 完成前无法运行真实集成
5. **Coverage 阈值未设置**: 当前不强制覆盖率门禁，待业务代码增长后设定
6. **contracts 子包未迁移**: schemas.test.ts 保持独立运行（tsx），未纳入 Vitest

## 后续依赖任务

- **M1-04**: migration-contract-test.mjs 集成到 T-STO-001
- **M2-03**: safety-policy-fixtures-v1.json 填充到 T-SAFE-001
- **M3-02**: BM25 fixture 填充到 T-RET-001（未来新增）
- **M4-02**: WS event fixtures 填充到 T-CON-001
- **M5-03**: provider-contract-fixtures-v1.json 填充到 T-PROV-001
- **M6-07**: Electron overlay 测试填充到 T-OVR-001
- **M7-04**: 全量 Contract/Integration tests 补全

## 追溯

- **文档**: DELIVERY §4.1（测试分层）, §4.4（testcase ID）, §8（DoD）
- **Testcase**: T-PKG-001（Windows CI）, T-CON-001, T-PROV-001, T-SAFE-001, T-DIAG-001, T-AUD-001, T-STO-001, T-CON-002, T-OVR-001
- **Acceptance**: 间接支持 A-01 ~ A-13 的自动化验证基础

## 注意事项

- 现有 `npm run test:contracts` 与 `npm run test:contract` 命名相似但功能不同：
  - `test:contracts` — 运行 src/contracts/test/schemas.test.ts（tsx）
  - `test:contract` — 运行 tests/contract/（Vitest）
- Windows CI 仅在 push 到 master/develop 或 PR 到 master 时触发
- Fixture 文件位于 docs/06-data-interface/fixtures/，不应在 tests/ 下复制
