项目路图：当前项目路图已保存在 `docs/08-delivery/Echocue-MVP里程碑与原子任务实施计划-v0.1.md`

正式开始任务之前，必须以该文件作为入口，强制要求每次只能领取一个任务。

每次任务所要读的材料需要渐进式披露，不得一次性读取大量非当前任务要求的材料内容。

每个任务完成之后，都需要明确在 `progress/` 目录下生成一个基于任务 ID 的文档，在文档中 Summary 当前任务做了什么、产出哪些文件。
然后在 `progress/README.md` 文档中明确标注每个任务的进度（增量标注，不需要一次性全部标注）以便下次进行任务回溯。